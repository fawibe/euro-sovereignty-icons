@font-face {
  font-family: "{{ name }}";
  src:
    url("./{{ name }}.woff2") format("woff2"),
    url("./{{ name }}.woff") format("woff");
  font-weight: normal;
  font-style: normal;
  font-display: block;
}
